var x = ("\"hello, world!\"") ; // assigned "hello world" to variable x

window.alert (x);    // gives a window alert of variable x, "hello world"

document.write ("this is the day " + "where i concatenate a string " + "and do it using javascript" ); // writes contents of paranthesis on home page with the contactenations

var y = ("          concatenated " + "string"); // Demonstrates contactenation in assiging a variable a value. 

document.write (y); //writes variable y on the page

var jill = " girl", jack = " boy", hill = " geographical feature", well = " architectural immplement"; // demonstrates assigning multiple variables on one line. 

document.write (jill); //writes one of the previously assigned variables

window.alert (3 + 3); //demonstrates simple mathmatics is possible when giving values. 

document.write(y.fontcolor ("blue"));